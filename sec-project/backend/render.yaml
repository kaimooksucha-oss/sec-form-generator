services:
  - type: web
    name: sec-form-api
    runtime: docker
    rootDir: backend
    dockerfilePath: ./Dockerfile
    envVars:
      - key: FRONTEND_URL
        value: https://YOUR-APP.vercel.app   # ← แก้เป็น URL จริงของคุณ
    plan: free
